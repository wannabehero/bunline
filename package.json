{
  "name": "bunline",
  "version": "0.0.1",
  "description": "A lightweight, durable, and fast worker queue for Bun, backed by bun:sqlite.",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "check": "biome check .",
    "fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
